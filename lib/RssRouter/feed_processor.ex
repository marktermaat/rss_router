defmodule RssRouter.FeedProcessor do
  def process_feed(uri) do
    # Fix dets storage to make the directory configurable
    # Get all items
    # Get latest timestamp/ID
    # Filter list and get new items
    # Send items to a publisher
  end
end
